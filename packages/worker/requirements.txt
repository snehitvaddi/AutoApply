supabase>=2.0.0
httpx>=0.27.0
playwright>=1.40.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
cryptography>=41.0.0
scrapling>=0.2.0
# curl_cffi is an optional dep of scrapling that its fetchers/requests.py
# imports eagerly — without this pin, Windows CI (and some clean installs)
# fail with ModuleNotFoundError the first time scout/__init__.py imports
# LinkedInPublicScout → scanner.linkedin → scrapling.fetchers.
curl_cffi>=0.7.0
python-jobspy>=1.1.0
beautifulsoup4>=4.12.0
# Claude-Agent-SDK powers packages/worker/brain/. It wraps the local
# `claude` CLI as a subprocess — auth comes from the user's Claude
# subscription (`claude login`), not ANTHROPIC_API_KEY. Requires Python
# >=3.10, which the installer provisions via homebrew python@3.11.
claude-agent-sdk>=0.1.60
# MCP Python SDK — used by brain/mcp_server.py to expose the 24 applyloop
# tools to PTY Claude Code via .claude/settings.json MCP config.
mcp>=1.0.0
