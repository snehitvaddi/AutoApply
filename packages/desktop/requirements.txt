fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
httpx>=0.28.0
ptyprocess>=0.7.0
pywebview>=5.0
# Required by POST /api/resumes/upload — FastAPI pulls this in at route
# registration time via File(...)/Form(...) deps, so the whole server
# crashes on import if it's missing (not a lazy error). Missing this
# dep was the cause of the v1.0.4 crash-on-launch bug.
python-multipart>=0.0.18
# Windows ConPTY backend — provides the same PTY interface as Unix pty.fork()
# but using Windows Pseudo Console (ConPTY). Only installed on Windows.
pywinpty>=2.0.0; sys_platform == "win32"
