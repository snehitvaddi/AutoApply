{
  "name": "@autoapply/desktop-ci",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright smoke tests that drive the packaged ApplyLoop desktop app against localhost:18790",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1"
  }
}
